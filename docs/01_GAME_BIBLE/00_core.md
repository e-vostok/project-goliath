---
module: 00_core
dependencies: []
tick_phase: ORCHESTRATOR
in_scope:
  - Регистрация и идентификация игрока через VK Launch Params
  - Управление сущностью «Государство» (создание, переименование, смена цвета, удаление)
  - Владение провинциями как учётными единицами (без геовизуализации)
  - Оркестрация цикла хода (Tick DAG): сбор запланированных действий, применение, инкремент счётчика
  - Собственный игровой календарь (производная функция от счётчика хода)
  - Универсальный примитив «механический деятельный акт» (постановка действия в очередь на тик)
out_of_scope:
  - Конкретные игровые механики модулей-сателлитов (экономика, армия, дипломатия и т.д.)
  - Визуализация карты и провинций (SVG-рендер, геометрия, смежность)
  - Уведомления через VK-бота (сообщество)
  - Расчёт любых показателей государства сверх названия, цвета и списка провинций
---

# 00_core.md

## ОБЩАЯ ЧАСТЬ

### 1. Идентификация и регистрация игрока

**Триггер:** открытие клиента VK Mini App пользователем.
**Условие:** валидная подпись параметров запуска (VK Launch Params, HMAC-SHA256) и свежесть временной метки запуска (`vk_ts`) в пределах допустимого окна.
**Системный эффект:** если профиль игрока с данным `vk_user_id` отсутствует — создаётся автоматически; в любом случае клиенту выдаётся сессионный токен, подтверждающий личность игрока для всех последующих запросов.

Отдельная форма логина/пароля в системе отсутствует. Идентификация происходит исключительно через платформу ВКонтакте.

**Граничные состояния:** недействительная подпись или истёкшая временная метка — запрос отклоняется, сессия не создаётся. Обновление истёкшего токена происходит прозрачно для игрока, без отдельного экрана логина.

---

### 2. Государство: создание

**Триггер:** подтверждение игроком формы регистрации государства.
**Условие:** игрок не владеет государством на момент подачи; указанное название государства уникально в рамках системы; указанный HEX-цвет уникален в рамках системы; все указанные ID провинций существуют в базе, уникальны между собой и свободны (не закреплены за другим активным государством).
**Системный эффект:** создаётся сущность государства, привязанная к игроку; каждая указанная провинция закрепляется за созданным государством.

Проверка географической смежности провинций не выполняется — анклавы допустимы.

**Граничные состояния:** нарушение любого из условий (занятое имя, занятый цвет, несуществующий/занятый/повторяющийся ID провинции) — операция отклоняется целиком; частичное закрепление провинций не допускается (атомарность).

---

### 3. Государство: изменение и удаление

**Триггер:** подтверждение игроком формы редактирования либо запроса на удаление.

**Условие (изменение):** игрок владеет активным государством; новое название/цвет либо совпадает с текущим значением, либо уникально в рамках системы.
**Системный эффект (изменение):** обновляются соответствующие поля государства.

**Условие (удаление):** игрок владеет активным государством; удаление подтверждено повторным явным действием игрока.
**Системный эффект (удаление):** запись о государстве удаляется из базы; закреплённые за ним провинции переходят в статус свободных.

**Граничные состояния:** удаление необратимо, механика отмены/восстановления отсутствует; интерфейс обязан требовать явное подтверждение перед выполнением.

---

### 4. Игровой цикл (ход)

**Триггер:** истечение сконфигурированного интервала реального времени с момента начала текущего хода.
**Условие:** наступление триггера не зависит от присутствия игроков в сессии; момент начала и окончания хода одинаков для всех государств.
**Системный эффект:** ядро считывает все запланированные за ход действия по всем модулям, применяет их в строгом порядке фаз Tick DAG (Environment → Production → Consumption → Resolve → Expiration), атомарно фиксирует итоговое состояние в базе данных, увеличивает счётчик хода на единицу.

Ядро `00_core` не является участником ни одной из 5 игровых фаз тика — оно выступает оркестратором, запускающим фазы по цепочке, и выполняет инкремент счётчика хода как финальный мета-шаг после завершения `PHASE_5_EXPIRATION`.

**Граничные состояния:** если игрок не совершил ни одного действия за ход — государство считается бездействующим, зависящие от решений игрока показатели не изменяются; регистрация и подключение новых игроков возможны в любой момент внутри хода без влияния на его границы.

---

### 5. Течение времени и календарь

**Триггер:** отсутствует — производная величина.
**Системный эффект:** игровая дата вычисляется как функция от счётчика хода и параметров календаря (точка отсчёта, соотношение игровых суток к одному ходу).
**Граничные состояния:** не применимо — значение детерминировано и не может рассинхронизироваться при корректной работе счётчика хода.

---

### 6. Механический деятельный акт (базовый примитив)

**Триггер:** взаимодействие игрока с элементом управления на странице государства или панели модуля.
**Условие:** конкретное ограничение по частоте выполнения (один раз за ход / многократно за ход / без ограничений / один раз за всё время игры) определяется правилами модуля, которому принадлежит акт; `00_core` предоставляет только универсальный механизм постановки в очередь и проверки частоты, но не сами правила частоты.
**Системный эффект:** изменение записывается как запланированное к применению в момент Tick и является окончательным решением государства.

**Граничные состояния:** попытка превысить разрешённую для акта частоту — запрос отклоняется на уровне API; механика отмены или отзыва уже принятого сервером акта отсутствует; потеря соединения до подтверждения сервером означает, что акт не считается принятым и должен быть повторён игроком.

---

### 7. Государство: отображаемые сведения

Страница государства отображает: название государства, HEX-цвет государства, список ID провинций, принадлежащих государству, номер текущего хода игры и производную от него игровую дату.

Расширение состава отображаемых показателей относится к зоне ответственности модулей-сателлитов и определяется их собственными Game Bible.

---

## ИНТЕРФЕЙС (VK Mini App)

- **Отдельный экран входа отсутствует.** Идентификация происходит автоматически при запуске клиента через VK Bridge/launch params; игрок без государства сразу видит панель создания государства вместо формы логина.
- **Панель создания государства:** поля — название государства (текст), цвет государства (HEX, выбор через цветовой пикер), список ID провинций (ручной ввод нескольких числовых значений); кнопка подтверждения; ошибки валидации (занятое имя/цвет/провинция) отображаются инлайн по конкретному полю.
- **Страница государства:** отображает сведения по п. 7; содержит элементы управления для перехода к редактированию названия/цвета и к удалению государства (удаление — через модальное окно подтверждения).
- **Панели механических деятельных актов**, специфичные для конкретных механик, принадлежат зоне ответственности модулей-сателлитов и в `00_core` не специфицируются.

---

## ГРАНИЧНЫЕ СОСТОЯНИЯ (сквозные для модуля)

- **Отсутствие связи с сервером:** клиент не выполняет локальных/офлайн операций; действие либо подтверждено сервером, либо не считается совершённым.
- **Превышение лимитов:** предельное количество провинций на одно государство и предельная длина текстовых полей — числовые диапазоны, не фиксируются в Game Bible и подлежат определению на этапе спецификации как конфигурируемые Pydantic-ограничения.
- **Нулевые значения:** текущими правилами не запрещено существование государства без ни одной провинции; данный вопрос требует явного решения на этапе спецификации.